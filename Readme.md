## Fluid Onboarding App

This is a fluid onboarding app that is built with React and Tailwind CSS.

Designed the full onboarding flow in Figma, then vibe coded it into a working React prototype.

## How to run the app

1. Clone the repository
2. Run `npm install` to install the dependencies
3. Run `npm start` to start the app